import { AddressValidatorPipe } from './address-validator.pipe';

describe('AddressValidatorPipe', () => {
  it('should be defined', () => {
    expect(new AddressValidatorPipe()).toBeDefined();
  });
});
